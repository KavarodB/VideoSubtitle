# Copy this file to .env and fill in your real values.
# Never commit .env — it's in .gitignore.

OPENAI_API_KEY=sk-...

# Optional tuning
# TMP_TTL_SECONDS=3600       # How long tmp files are kept (default: 1 hour)
# TMP_CLEAN_INTERVAL=600     # Cleanup check interval in seconds (default: 10 min)
